<%
var scriptFile = "loc-en_US.js"; // default

if (typeof lang === "string") {
  var l = lang.toLowerCase();

  if (l === "es") {
    scriptFile = "loc-es_419.js";
  } else if (l === "fr") {
    scriptFile = "loc-fr_CA.js";
  } else if (l === "ja") {
    scriptFile = "loc-ja_JP.js";
  } else if (l === "en") {
    scriptFile = "loc-en_US.js";
  }
}
%>

  <script src="/js/<%= scriptFile %>?v=19" onerror="handleScriptError('<%= scriptFile %>')"></script>
